# Política de privacidad base

Esta versión no usa login, backend, analíticas ni publicidad. El progreso se guarda localmente en el navegador mediante `localStorage`.

Si el usuario borra datos del navegador, cambia de dispositivo o usa modo incógnito, puede perder el progreso local.
